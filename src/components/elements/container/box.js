import styled, { space, color, layout, border, background, position } from '@style'

export const Box = styled.div`
    ${space}
    ${color}
    ${layout}
    ${border}
    ${background}
    ${position}
`
